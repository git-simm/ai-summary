server:
  port: 8080

spring:
  application:
    name: mcp-client
  profiles:
    active: mcp-client
  ai:
    deepseek:
      api-key: sk-ba120f394f5a457cb948ef8238676ebc
      chat:
        base-url: https://api.deepseek.com
        completions-path: /v1/chat/completions
        options:
          model: deepseek-chat
